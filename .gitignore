# Secrets and live browser session state — never commit
.env
accounts/
runtime_config.json
content_queue/
scheduled/
data_sync_cache/
screenshots/
human_bot.db
# scratch Playwright Codegen recordings — merged into human_bot/actions.py, not kept
codegen_*.py
__pycache__/
*.pyc
.venv/
.DS_Store
