# Dev-only dependencies — not needed to run the service, only to run the
# test suite in tests/. Install alongside requirements.txt when working
# on this project: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.24
